# Core dependencies for thebrain-mcp
fastmcp>=1.0.0
httpx>=0.27.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Install the package itself from GitHub
# (FastMCP Cloud should run this from the python/ directory)
git+https://github.com/lonniev/thebrain-mcp.git#subdirectory=python
